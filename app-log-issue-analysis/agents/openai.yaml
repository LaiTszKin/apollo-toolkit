interface:
  display_name: "App Log Issue Analysis"
  short_description: "Analyze logs with code context to detect issues"
  default_prompt: "Use $app-log-issue-analysis to inspect application logs with code context, identify evidence-backed issues, suggest concrete remediation steps, and publish confirmed findings to GitHub Issues with auth fallback (gh login first, token second) and remote README-based issue language selection."
