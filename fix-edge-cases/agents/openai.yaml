interface:
  display_name: "Edge Case Discovery"
  short_description: "Discover reproducible edge-case risks and coverage gaps"
  default_prompt: "Use $fix-edge-cases to scan the current diff first (or the full codebase when there is no diff), identify the highest-risk reproducible edge-case findings, validate them with concrete evidence, prioritize the confirmed risks, and report hardening and test recommendations without modifying code."
